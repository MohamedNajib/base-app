import 'package:flutter/material.dart';

class Constant {
  static const Duration duration200 = Duration(milliseconds: 200);
  static const Duration duration375 = Duration(milliseconds: 375);
  static const Duration duration300 = Duration(milliseconds: 300);

  static const radiusAll20 = BorderRadius.all(Radius.circular(12));
  static const radius12 = Radius.circular(12);
}
